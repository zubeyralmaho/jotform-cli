version: "2"

run:
  timeout: 5m
  go: '1.24'

linters:
  exclusions:
    paths:
      - ^docs/external/jotform-api-go/.*\.go$
    rules:
      - path: ^docs/external/jotform-api-go/.*\.go$
        linters:
          - errcheck
